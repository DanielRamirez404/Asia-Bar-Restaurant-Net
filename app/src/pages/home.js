import react from "react";
import Dashboard from "./reusables/dashboard-page";

const Home = () => {
    return (
        <Dashboard content={ (<h1 style={{textAlign: "center"}}>Home</h1>) } />
    );
}

export default Home;