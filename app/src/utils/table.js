export default class Table {
    constructor(name, dbname, fields, dbfields) {
        this.name = name; 
        this.dbname = dbname;
        this.fields = fields;
        this.dbfields = dbfields;
    }
}
